services:
  db: 
    image: postgres:18
    restart: always
    env_file:
      - .env
    environment:
      PGDATA: /var/lib/postgresql/18/docker
    ports:
      - 5432:5432
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
